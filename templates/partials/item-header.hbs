<header class="sheet-identity item-identity"><img src="{{item.img}}" data-edit="img" title="{{item.name}}" alt="{{item.name}}"/><div><p class="sheet-kicker">{{localize placeholder}}</p><input name="name" type="text" value="{{item.name}}" placeholder="{{localize placeholder}}"/></div></header>
